<div id="clusterdata">
	<div class="container">
		<div class="row">
			<div class="col-sm-10 col-sm-offset-1">
				<h2>Clusterduck Incoming Data</h2>
				<div class="cluster-data">
					<%= @clusterdata.each do |c| %>
						<p><%= c.content.to_s %><p>
					<% end %>
				</p>
			</div>
		</div>
	</div>
</div>